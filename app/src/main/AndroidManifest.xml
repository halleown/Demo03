<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Demo03">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".coroutine.Activity1" />
        <activity android:name=".coroutine.Activity2" />
        <activity android:name=".coroutine.Activity3" />
        <activity android:name=".tree.Activity4" />
        <activity android:name=".mytree.TreeActivity" />
        <activity android:name=".scan.QRCodeScanActivity" />
        <activity android:name=".graph.GraphActivity" />
        <activity android:name=".form.CustomFormActivity" />
    </application>

</manifest>